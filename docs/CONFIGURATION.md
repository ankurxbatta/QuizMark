# Configuration

All settings live in `.env`. Run `setup.sh` (Mac/Linux) or `setup.bat` (Windows) to create it automatically, or copy `.env.example` manually.

---

## Required Variables

| Variable | Description |
|---|---|
| `SECRET_KEY` | Signs JWT tokens. Auto-generated by setup script. Use `python3 -c "import secrets; print(secrets.token_hex(32))"` to generate manually. |
| `ADMIN_PASSWORD` | Instructor login password. Required. |
| `GEMINI_API_KEY` | Free-tier embeddings. Get at [aistudio.google.com](https://aistudio.google.com/app/apikey). |
| `OPENAI_API_KEY` | Primary LLM provider (vision, math, generation, marking). Get at [platform.openai.com](https://platform.openai.com/api-keys). |
| `ANTHROPIC_API_KEY` | Fallback LLM provider. Get at [console.anthropic.com](https://console.anthropic.com). |

---

## Environment

| Variable | Default | Description |
|---|---|---|
| `ENVIRONMENT` | `development` | `development` or `production`. In production the API docs (`/docs`, `/redoc`) are disabled and demo-data seeding refuses to run. |

---

## Auth

| Variable | Default | Description |
|---|---|---|
| `SECRET_KEY` | — | JWT signing secret. Required. |
| `JWT_ALGORITHM` | `HS256` | JWT algorithm. Do not change. |
| `JWT_EXPIRY_MINUTES` | `30` | Token lifetime in minutes. |
| `MAX_FAILED_LOGIN_ATTEMPTS` | `3` | Failed logins before account lockout. |
| `LOCKOUT_DURATION_MINUTES` | `5` | Lockout duration. |
| `ADMIN_ENABLED` | `true` | Creates admin account on first startup. |
| `ADMIN_USERNAME` | `admin` | Admin login username. |
| `ADMIN_PASSWORD` | — | Admin login password. Required. |
| `ADMIN_ROLE` | `instructor` | Role assigned to the admin account. |

---

## Gemini (Embeddings)

| Variable | Default | Description |
|---|---|---|
| `GEMINI_API_KEY` | — | Required. Used for 768-dim embeddings only. |
| `GEMINI_EMBEDDING_MODEL` | `gemini-embedding-001` | Do not change — MongoDB index is built on 768-dim output of this model. |

---

## OpenAI (Primary LLM)

| Variable | Default | Description |
|---|---|---|
| `OPENAI_API_KEY` | — | Required. |
| `OPENAI_EMBEDDING_MODEL` | `text-embedding-3-small` | Fallback embeddings (768-dim via `dimensions=768`). |
| `OPENAI_VISION_MODEL` | `gpt-4o-mini` | Chart and image descriptions. |
| `OPENAI_GENERATION_MODEL` | `gpt-4o-mini` | Question generation. |
| `OPENAI_MARKING_MODEL` | `gpt-4o-mini` | Answer marking. |

---

## Anthropic (Fallback LLM)

| Variable | Default | Description |
|---|---|---|
| `ANTHROPIC_API_KEY` | — | Required. Activates automatically when OpenAI hits quota. |
| `ANTHROPIC_VISION_MODEL` | `claude-haiku-4-5-20251001` | Chart vision fallback. |
| `ANTHROPIC_GENERATION_MODEL` | `claude-haiku-4-5-20251001` | Generation fallback. |
| `ANTHROPIC_MARKING_MODEL` | `claude-haiku-4-5-20251001` | Marking fallback. |

---

## LLM Routing

| Variable | Default | Description |
|---|---|---|
| `GENERATION_LLM_ENABLED` | `true` | Enable LLM question generation. |
| `GENERATION_LLM_PROVIDER` | `openai` | Primary generation provider. |
| `GENERATION_LLM_MODEL` | `gpt-4o-mini` | Generation model. |
| `GENERATION_MAX_TOKENS` | `4096` | Max tokens per generation call. |
| `ONLINE_LLM_ENABLED` | `true` | Enable LLM answer marking. |
| `ONLINE_LLM_PROVIDER` | `openai` | Primary marking provider. |
| `ONLINE_LLM_MODEL` | `gpt-4o-mini` | Marking model. |
| `LLM_TEMPERATURE` | `0.2` | Generation temperature. |
| `LLM_MAX_TOKENS` | `1024` | Max tokens per marking call. |

---

## MongoDB

| Variable | Default | Description |
|---|---|---|
| `MONGODB_ENABLED` | `true` | Enable MongoDB. Always true. |
| `MONGODB_URL` | `mongodb://mongodb:27017/?directConnection=true` | Connection string. Uses Docker service name `mongodb`. |
| `MONGODB_DB_NAME` | `marking_tools` | Database name. |

---

## Celery / Redis

| Variable | Default | Description |
|---|---|---|
| `CELERY_BROKER_URL` | `redis://broker:6379/0` | Redis task queue URL. |
| `CELERY_RESULT_BACKEND` | `redis://broker:6379/0` | Redis result storage URL. |

---

## Marking Confidence Thresholds

| Variable | Default | Description |
|---|---|---|
| `CONFIDENCE_HIGH` | `0.85` | SLM score ≥ this → mark accepted without LLM call. |
| `CONFIDENCE_MID` | `0.55` | Score between MID and HIGH → RAG + OpenAI marking. Score below MID → wide RAG + OpenAI + flagged for review. |

---

## PDF Ingestion

| Variable | Default | Description |
|---|---|---|
| `UPLOAD_MAX_SIZE_MB` | `25` | Maximum PDF upload size. |
| `PDF_MAX_PAGES` | `700` | Maximum pages to process per PDF. |
| `PDF_MIN_CHUNK_CHARS` | `300` | Target minimum chunk size. Short tails are merged into the previous chunk (never silently dropped). |
| `PDF_MAX_CHUNK_CHARS` | `3000` | Chunks longer than this are split recursively. |
| `INGEST_PAGE_WINDOW` | `6` | Pages processed per checkpoint window. |
| `INGEST_TIME_BUDGET_SECONDS` | `1500` | Worker re-queues the task after this long (stays under Celery 30-min soft limit). |
| `ENABLE_VISION_EXTRACTION` | `true` | Enable chart/image vision descriptions. |

---

## Ingestion Chain (LangChain pipeline)

Each checkpoint window runs the chain clean → chunk → validate → vision → embed.
Failed windows are rolled back and retried on resume, so partial output never leaks into the store.

| Variable | Default | Description |
|---|---|---|
| `CHUNK_OVERLAP` | `200` | Character overlap between recursively split chunks. |
| `SEMANTIC_CHUNK_RATIO` | `0.2` | Share of chunks (by teaching density) re-split at semantic boundaries. |
| `SEMANTIC_MIN_CHARS` | `1500` | Only chunks at least this long are semantic-split candidates. |
| `INGEST_VISION_CONCURRENCY` | `6` | Parallel vision API calls per window. |
| `ENABLE_CHUNK_VALIDATION` | `true` | LLM math repair of formula chunks before DB insert (cached by content hash). |
| `VALIDATION_CONCURRENCY` | `4` | Parallel validation LLM calls. |
| `EMBEDDING_BATCH_SIZE` | `100` | Texts per embedding API call; failed batches fall back to per-item embedding. |
| `GEMINI_EMBEDDING_DELAY_SECONDS` | `0.5` | Pause between Gemini embedding calls (free-tier pacing). |
| `GEMINI_VISION_DELAY_SECONDS` | `0.0` | Pause between Gemini vision calls. |

---

## Specialist RAG Indexes (MULTI_RAG_DESIGN)

Per-modality retrieval indexes built asynchronously by the specialist workers
after a book finishes ingesting (and auto-backfilled on startup for existing books).

| Variable | Default | Description |
|---|---|---|
| `MATH_INDEX_ENABLED` | `true` | Per-formula index (`math_index`) built by worker-math. L3 (Apply) question generation retrieves exact formulas from it. |
| `FIGURE_INDEX_ENABLED` | `true` | Per-figure index (`figure_index`) built by worker-vision from stored chart descriptions. L4 (Analyze) generation retrieves real figures from it. |
| `TABLE_INDEX_ENABLED` | `true` | Per-table index (`table_index`) built by worker-clean from stored markdown tables. L4 (Analyze) generation retrieves real tables from it. |
| `INDEX_BUILD_BATCH_SIZE` | `10` | Items per enrichment LLM call during index builds (results cached by content hash). |

Disabling any index falls back to chunk-only retrieval for that modality.

---

## RAG Retrieval

| Variable | Default | Description |
|---|---|---|
| `TOP_K_RETRIEVAL` | `5` | Chunks retrieved per query in standard RAG. |
| `TOP_K_WIDE_RETRIEVAL` | `10` | Chunks retrieved when confidence is low (wider context). |
| `SIMILARITY_THRESHOLD` | `0.75` | Minimum cosine similarity for a chunk to be included in results. |

---

## Question Generation

| Variable | Default | Description |
|---|---|---|
| `GEN_CHAPTER_CONCURRENCY` | `5` | Chapters generating questions in parallel. |
| `DEDUP_SIMILARITY_THRESHOLD` | `0.92` | Embedding cosine ≥ this → question is a near-duplicate and dropped. |

---

## CORS

| Variable | Default | Description |
|---|---|---|
| `CORS_ORIGINS` | `http://localhost:3000` | Comma-separated list of allowed frontend origins. Add your production domain here. |
